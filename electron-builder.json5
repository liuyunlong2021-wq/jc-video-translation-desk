// @see - https://www.electron.build/configuration/configuration
{
  $schema: 'https://raw.githubusercontent.com/electron-userland/electron-builder/master/packages/app-builder-lib/scheme.json',
  productName: '视频翻译工作台',
  appId: 'com.yils.video-translation-workbench',
  asar: true,
  directories: {
    output: 'release-translation/${version}',
  },
  files: ['dist', 'dist-electron', 'dist-native/better-sqlite3.node', 'locales', 'skills'],
  extraResources: [
    { from: 'runtime/funasr', to: 'funasr', filter: ['**/*', '!**/__pycache__/**', '!**/*.pyc'] },
    { from: 'dist-native/runtime-tools', to: 'runtime-tools', filter: ['**/*'] },
  ],
  npmRebuild: false, // disable rebuild node_modules 使用包内自带预构建二进制，而不重新构建
  beforePack: './build/scripts/before-pack.js',
  mac: {
    target: [
      {
        target: 'dmg',
        arch: ['universal'],
      },
    ],
    artifactName: '视频翻译工作台-${version}-mac-${arch}-installer.${ext}',
    icon: './public/video-translation-icon.png',
  },
  win: {
    target: [
      {
        target: 'nsis',
      },
    ],
    artifactName: '视频翻译工作台-${version}-win-${arch}-setup.${ext}',
    icon: './public/video-translation-icon.png',
  },
  nsis: {
    language: '0x0804',
    oneClick: false,
    perMachine: false,
    allowToChangeInstallationDirectory: true,
    deleteAppDataOnUninstall: false,
  },
  linux: {
    target: ['AppImage'],
    artifactName: '视频翻译工作台-${version}-linux-${arch}.${ext}',
    icon: './public/video-translation-icon.png',
  },
  publish: null, // disable auto publish 防止重复发版
}
