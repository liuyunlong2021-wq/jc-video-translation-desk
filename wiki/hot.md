# Hot

## 当前重点

- [[产品/视频翻译工作台/配音工作台]] 是当前最新产品状态页。
- [[产品/视频翻译工作台/成片工作台]] 是第三页成片链路的最新产品状态页。
- 第二页配音工作台已形成主链路：角色配音 → 全局配音提示词 → 分组配音提示词 → 选组生成配音 → 试听 → 进入成片工作台。
- 成片工作台默认主按钮已收口为 `生成成片`：消费第二页完整 grouped 分组克隆配音，自动串联配音对白时间戳、原声/背景分离、目标语言音轨合成、字幕配音烧录。
- `合成目标语言音轨` 已改为看真实可用产物：`targetVoicePath` 或完整 grouped 分组克隆版本，不再被参考音绑定或 `voiceStatus` stale 阻断。
- 分组配音生成采用单并发排队，解决 Seed Audio 并发 429。
- `生成配音` 的语义是“生成当前选中的配音组”；全选即全部排队生成，未选择时默认补跑未完成组。
