# 本地构建与发布

## 环境

- Node.js：项目要求 `>=22.17.0`。
- pnpm：项目锁定 `10.12.4`。
- Electron：`22.3.27`。
- FFmpeg：使用项目已安装的 `ffmpeg-static`，构建版从 `app.asar.unpacked` 解析。

## 常用命令

```bash
pnpm test
pnpm build
open release-translation/1.2.2/mac-universal/视频翻译工作台.app
```

`pnpm build` 会先执行 `build/scripts/clean-build.js`，删除 `dist/`、`dist-electron/` 和当前版本的 `release-translation/<version>/`，再进行类型检查、Vite 构建和 Electron 打包。

Windows 安装包必须在 Windows 电脑或 GitHub Actions 的 `windows-latest` Runner 构建。不要在 Mac 上交叉构建 Windows 包；`ffmpeg-static` 按构建机系统安装，交叉构建会带入错误平台的 FFmpeg。

## 安装包平台规则

- macOS 默认产物为 `视频翻译工作台-<version>-mac-universal-installer.dmg`。Universal 包同时包含 `arm64` 与 `x64`，Apple M 系列和 Intel Mac 使用同一个 DMG，不需要分别发布。
- Windows 安装包只能在 Windows 电脑或 GitHub Actions `windows-latest` Runner 生成。
- Linux 安装包在 GitHub Actions `ubuntu-latest` Runner 生成。
- GitHub Release 从 `release-translation/<version>/` 上传 `.dmg`、`.exe`、`.deb`、`.rpm` 和 `.AppImage`，不得继续读取旧 `release/<version>/`。

## 数据位置

生成媒体不存放在仓库。用户选择或新建的空文件夹本身就是项目根，项目 Wiki、状态、输入、资产和媒体都位于该目录：

```text
<用户选择的项目根>/
  project.json
  shared-state.json
  episodes/
  assets/
  inputs/
  wiki/
```

Electron `userData` 统一位于系统应用数据目录下的 `jc-video-translation-desk/`，只保留 `media-projects.json` 项目位置注册表、API Key、数据库、FunASR 模型与环境和全局声音库。退出或替换应用包不应删除用户项目根；排障时也不应通过清理 `userData` 解决构建产物问题，避免丢失注册表和应用级配置。

首次切换到独立 APP 时，只从 `视频翻译工作台`、`短视频工厂`、`short-video-factory`、`AI Short Video Factory` 旧目录复制新目录缺失的项目注册表、API Key、SQLite、声音库和本地 TTS 数据。已有新数据不覆盖，旧目录不删除，便于回退。

FunASR 安装器、识别进程和 SQLite 都以 Electron 当前 `userData` 为准，避免模型、环境和数据库分别落入不同产品目录。

## 发布前最小验收

```bash
pnpm exec vue-tsc --noEmit
pnpm test
pnpm build
git diff --check
```

2026-08-10 已验证 `200/200` 自动测试、类型检查、macOS Universal 正式构建和桌面 APP 核验通过。运行日志中的数据库路径为 `.../jc-video-translation-desk/data.db`；配音工作台固定显示“生成角色提示词”“按提示词生成角色参考音”“进入成片工作台”三个按钮。

## 来源与追溯

- 来源角色：2026-08-10 用户确认、并发代码审计、当前工作区实现与真实构建日志。
- `electron/main.ts`：`sha256:6ef22e974718fb6593b76c4f8811c7d4a906aae4acd361e177439435a94baca0`。
- `src/views/Home/components/VideoRender.vue`：`sha256:f15b276c430995792d47382ab61649e8e1bfb2a4015adb5444f3b4777d425d54`。
- `src/views/Home/index.vue`：`sha256:f538462ced30edbb105e8a985de057409c71a7630c94721e2b119737996db100`。
- `.github/workflows/build.yml`：`sha256:6f7b0c05991a0641f058e25e15c762d3ffdc969c82f93944972431d2cd83a3b0`。
- `package.json`：`sha256:1710083091cea9ec7b03c32f9d1ecb2ff3520e298054594c4329a4a4b24b79a8`。
- `electron-builder.translation.json5`：`sha256:4692ef0f0a4b4d5a394797b169627ce60590ee5f47ae924c7917662c434b4eff`。
- 当前会话指纹：未计算（会话不是项目内文件）。
- 已处理范围：产品身份、数据目录、旧数据兼容、按钮门禁、三平台构建与发布；未把 Windows/Intel 干净机器首装写成已实测。

## 启动规则

- 启动新包前完全退出旧 Electron 进程。
- 进程检查必须按 `jc-video-translation-desk` 或视频翻译工作台应用的完整路径过滤，避免影响其他 Electron 应用。
- Node 25 的历史 `localStorage` 兼容问题及测试入口处理见 [[排障/开发环境localStorage兼容问题]]。

## macOS 签名

本机发布证书若在 `codesign` 阶段返回 `errSecInternalComponent`，可先生成仅供本地验证的未签名包：

```bash
CSC_IDENTITY_AUTO_DISCOVERY=false pnpm build
```

该命令不会修改项目配置。正式分发仍应修复钥匙串或证书环境后执行普通 `pnpm build`。

## GitHub 推送经验

本机全局 Git 配置使用 `http.sslbackend=openssl` 时，`git push` 和 `git ls-remote` 可能在 75 秒后连接 GitHub 443 超时，而普通 `curl` 访问正常。已验证的一次性绕行命令为：

```bash
git -c http.sslbackend=secure-transport push origin main
```

该命令不修改全局配置。2026-07-30 使用它成功推送提交 `57c48b6`。

构建动态分块问题见 [[排障/构建产物与运行进程版本不一致]]。
