# 视频翻译工作台端到端验收

> 状态：导入 SRT、硬字幕 OCR、FunASR 固定时间戳、画面识别人物、人工确认、翻译和整段配音英文词级对齐主链已由真实样片/自动测试验证
>
> 更新：2026-08-16

## 当前结论

旧的“压缩视频、全片多模态理解、切片、逐片扒片”字幕路线已经退出。现行字幕主链按用户可理解的字幕来源分三类：导入 SRT 直接作为权威字幕；上传有字幕视频走硬字幕 OCR 生成带时间戳的 SRT；上传无字幕视频才提取整轨音频并用 FunASR 生成固定时间戳和原始文字。最终仍以人工确认稿翻译。

真实样片已确认：FunASR 时间戳可直接采用；按真实字词时间戳拆分过长字幕后显示效果成功；硬字幕 OCR 在有字幕视频中能直接获得更准确的字幕文本；画面人物聚类在样片中明显比声音聚类更接近真实角色数量，因此角色工作流采用“画面优先、声音兜底、人工最终确认”。完整英文配音也已验证可以由 FunASR 整块识别，再将确认英文稿与英文词级时间按顺序对齐后剪句；自动测试覆盖 FunASR 把一句拆成多个识别 cue 的情况。

现行执行合同以 [视频翻译工作台“识别字幕”执行流程](../../视频翻译工作台-扒片按钮执行流程.md) 为准。TDD-13 与本页后文旧切片路线只保留历史追溯，不再作为字幕识别实现依据。

## 2026-08-16 硬字幕 OCR 主链验收

- 字幕来源 UI 固定为用户可理解的三类：`导入SRT`、`上传有字幕视频`、`上传无字幕视频`。主操作统一为 `获取字幕`，不再把“抽帧校准”作为获得字幕的主入口。
- `导入SRT` 是最高优先级：已有 SRT 直接导入并跳过 ASR/OCR，因为本产品后续语音识别、大模型校准和语义校准的目的都是得到准确带时间戳字幕。
- `上传有字幕视频` 使用 `VideoSubFinder -> RapidVideOCR/RapidOCR -> SRT`。VideoSubFinder 负责从视频中提取硬字幕关键帧，OCR 引擎负责识别文字并生成字幕。
- `上传无字幕视频` 继续使用音频 ASR 路线：FFmpeg 提取音频，FunASR 识别声音和时间戳，再进入人工确认稿。
- 角色识别从旧字幕校准中拆出，保留为“画面识别人物”：在已有准确字幕后，大模型可以结合 SRT 上下文、声音片段和多帧画面判断当前 cue 的角色候选，但不能替代字幕事实源。
- 本轮真实样片 `D:\0816翻译测试\episodes\episode-001\video-translate\source.mp4` 发现 VideoSubFinder 默认下方区域会漏掉竖屏短剧嵌入横屏画面时的字幕。根因是字幕位于有效画面下方，而不是完整视频帧的最底部。
- 已增加 RapidOCR 兜底：先检测有效非黑画面区域，再对有效画面的中下区域做 OCR，保留下方横排文本并合并相近字幕。该路线在上述样片中识别出 26 条 cue，用户确认测试结果“非常成功”。
- 当前边界：OCR 兜底仍比纯 ASR 慢，88 秒样片约 80-94 秒；下一轮需要把进度文案简化为“正在获取画面字幕”，并继续调窄有效画面下方的 OCR 截取区域。

## 2026-08-10 分组克隆：人工确认稿是唯一事实源

- `sourceText` 和 `translatedText` 组成字幕工作台的人工确认稿，是分组克隆、全局提示词、分组提示词、配音版本与成片字幕的唯一文本来源；`recognizedText`、抽帧建议、语义校准建议和旧提示词都不是并行稿件。
- 字幕合并、译文修改、角色绑定、时间轴调整或语种变化会废弃全局提示词、分组提示词与下游配音采用状态。用户必须基于当前人工确认稿重新生成全局提示词，系统不得用旧提示词校验新稿。
- 只调整“与下一条组成配音组”或“从当前配音组拆出”时，不改变人工确认稿，也不废弃全局提示词；仅清除分组提示词和当前采用的分组配音版本。
- 分组克隆不再显示源视频预览。中栏只保留可滚动的逐条字幕表格，显示与字幕工作台一致的 `#序号`、时间轴、角色、人工确认稿、译文字幕、配音组与状态；字幕本身永远不被配音分组操作合并。
- 当前组的三段式提示词移动到右侧操作栏，固定四行高度、可直接编辑。编辑立即写回当前分组提示词，并使已采用的分组配音版本失效；下一次单组或批量生成使用修改后的文本。
- 旧提示词与当前人工确认稿不一致时，界面只显示“人工确认稿已变化，请重新生成全局配音提示词”，不向用户暴露 `voice-block-*` 的内部校验错误。

## 2026-08-10 验证

- 用户确认本轮分组克隆交互调整“非常成功”。已完成桌面核验：视频预览移除、字幕表格获得完整主区域、配音组保留 `组02 / 字幕02-03` 关系、旧提示词错误转换为可执行提示。
- `pnpm test`：`206/206` 通过；`pnpm exec vue-tsc --noEmit`、`git diff --check` 通过。
- macOS Universal 应用签名与 DMG 校验通过；产物为 `release-translation/1.2.2/视频翻译工作台-1.2.2-mac-universal-installer.dmg`。

## 现行已跑通主链

```text
上传或更换识别视频
-> FFmpeg 提取 16 kHz 单声道 WAV
-> FunASR 整轨识别声音、时间戳、匿名说话人和情绪候选
-> 程序只按真实字词时间戳拆分过长字幕
-> 保留不可变 FunASR 原文，生成可手工编辑的人工确认稿
-> 按需对每条 cue 的时间中点抽一帧
-> 同一次画面模型调用给出字幕文字建议和 visual-person-N 聚类
-> 按需让大模型结合 FunASR、画面建议和整集上下文进行语义校准
-> 人工应用建议、撤销、恢复原文或直接手写
-> 以人工确认稿翻译，时间戳始终不变
-> 人工复核后进入配音工作台
```

## 2026-08-09 已验证的成功经验

- 时间轴与文字分权：FunASR 提供时间锚点，抽帧和语义模型只提供文字建议，不允许模型返回或改写时间戳、cue 数量、ID 和顺序。
- 原始证据与工作稿分权：`recognizedText` 永久保存 FunASR 原文；`frameSuggestion` 和 `calibrationSuggestion` 是可选辅助；`sourceText` 人工确认稿是翻译的唯一正文来源。
- 辅助功能不设门禁：只要人工确认稿非空，就能直接翻译；用户可以完全不用抽帧或语义校准，也可以只用其中一个。
- 一帧承担两项任务：每条 cue 只抽 `(startMs + endMs) / 2` 的一张中点帧，同一批图片同时读取画面字幕和聚类画面人物，没有新增第二轮抽帧或全片多模态分析。
- 画面人物与声音人物必须独立编号：`visual-person-N` 不能冒充 `speaker-N`，两者最终都只能绑定到产品自己的 `translationRoleId`。
- 角色选择采用画面优先：完成抽帧校准后，单人画面的候选、新建角色默认名和自动绑定使用“画面人物 N”；多人、无人或遮挡画面不猜，交给人工；未做画面校准时才回退声音候选。
- 单条和批量修改显式分开：角色下拉默认只改当前 cue；用户主动勾选“相同声音”或“相同画面人物”才批量应用，并显示影响条数。
- 用户界面只展示决策所需证据：一张实际中点帧、“声音人物 N”和“画面人物 N”；人物外观特征只作为模型内部聚类依据，不展示英文内部 ID 或“疑似人物”结论。
- 翻译使用整集上下文，不做孤立逐句直译。英语目标明确面向美国观众，要求当代美式影视口语、人物身份与情绪一致、对白适合原时间窗阅读和配音，同时禁止编造、遗漏、合并或拆分 cue。
- 角色变化不删除已有译文，只把译文标记为需要复核；人工确认稿变化才使旧译文失效。
- 抽帧校准按每批 20 条 cue 拆分为当前项目任务，批次结果落盘；失败后重新执行会复用已成功批次，只补跑失败或缺失批次。
- 真实样片结果：用户确认字幕拆分成功、时间戳满意、抽帧文字校准效果“出奇地好”；同一样片的画面人物聚类明显比声音聚类更接近实际三名角色。

## 自动验证边界

- Git 实现基线：`99fa67c`，tree `e6873847885767b5071e8441f17bffe0f18fb4f7`。
- `pnpm test`：`199/199` 通过；`pnpm exec vue-tsc --noEmit` 与 `git diff --check` 通过。
- 面向美国观众的新翻译 Prompt 和“画面优先、声音兜底”的最终候选显示已经通过代码与自动测试，仍需用户用下一次真实翻译结果继续评价措辞质量和交互体验。

## 整段配音对齐经验

成功方案：完整配音块只识别一次，不再预先切成固定 10 秒小片，也不再把音频交给多模态模型猜 `cueId`。FunASR 返回英文词级时间后，程序将确认英文台词和识别词流做字符级单调匹配：cue 顺序固定，首个匹配词确定开始，最后一个匹配词确定结束，再从原始高质量配音块用 FFmpeg 裁句。这样保留整段 Seed 的连续表演，同时避免逐句生成造成的断裂。

失败经验：旧的“固定小片 + 模型猜每片属于哪句”会在切片边界重复识别、漏掉尾部或把一句截成只有前几个音；不能作为整段配音对齐方案。把音频转成视频/图片再截帧也不会增加声音时间信息，不能解决英文发音与字幕的对应问题。

边界经验：极短的 `Mm.`、`Uh.` 等语气词可能没有可用识别词，程序保留前后真实词之间的完整声音区间；有识别词但文本匹配低于阈值时必须阻止进入成片并提示人工确认。当前仍需留意相邻 cue 的防截尾余量可能带来尾词重复，后续只能收紧相邻边界，不能回退到固定切片。

## 安装部署经验

FunASR 的 Python 环境和模型不提交仓库，也不包含在 DMG/安装包中。首次安装必须在源码目录执行 `pnpm setup:funasr`，该脚本会锁定 FunASR 提交、创建独立 Python 3.10 环境、安装依赖、下载模型并运行真实探针。Apple Silicon 已完成真实验证；Intel Mac 和 Windows 仍需在干净机器完成首装验收。README 已按 M 系列 Mac、Intel Mac 和 Windows 分成逐步的小白安装流程。

## 历史成功经验（旧切片路线，不再执行）

- 上传与处理分离：上传只归档原片；压缩、模型调用和切片只在用户点击“扒片”后发生。
- 原片与分析片分工明确：小文件直接分析，大文件生成较小分析副本；Gemini 全片分析和 FFmpeg 复核切片读取同一个分析视频，最终成片始终读取原片。
- 云端与本地阶段分开：Gemini、翻译模型和 Seed Audio 是可失败、可重试的云端任务；FFmpeg 切片、混音和烧录是本地任务，不能用云端错误描述本地阶段。
- 扒片保持简单：全片剧情与切点、FFmpeg 实际切片、Gemini 逐片台词、程序最终稿四阶段足够，不再叠加无效的多轮模型互审。
- 每阶段均有 Markdown 证据：`01-整体分析与切片方案.md`、`02-FFmpeg切片.md`、`03-Gemini逐片台词.md`、`04-最终稿.md` 形成可追溯链；JSON 只承担程序状态和执行合同。
- 字幕排版由程序决定：标点后强制形成新 cue，中日韩约 15 字、其他语言约 40 字，每行保留连续时间戳，不把换行交给模型自由发挥。
- 翻译角色库按项目跨集复用，但角色、声音、剧集产物和执行状态均留在 `wiki/翻译/` 与翻译媒体沙箱，不写回创作工作流。
- 声音身份只使用产品自己的 `translationRoleId` 和 `voiceProfileId`。正式配音请求直接携带当前参考音文件，并使用 `voiceProfileId` 完成参考音与 `@音频N` 的确定性映射。
- 英语配音稿采用“中文导演说明 + 英文正式对白”：用户能读懂并修改情绪、强度、语速和停顿，英文台词仍逐字保留。
- 大任务必须显示阶段进度、持续生成文档并支持失败后续跑；已完成的本地和云端产物不重复执行。

## 当前不做

- 不恢复压缩分析视频、全片多模态切点、物理切片、逐片扒片、分组分析或多轮模型互审。
- 不让画面模型或语义模型修改 FunASR/人工时间戳。
- 不让声音或画面模型直接决定真实角色姓名；正式角色始终由用户确认。
- 不立即决定整段 Seed 或逐句克隆谁是最终路线，先用相同输入做真实对比。

## 下一步：同集配音 A/B

使用同一集已确认的英文字幕、角色映射和参考音，保留现有成片参数，只比较两种目标人声生成方式：

| 路线             | 本轮动作                                                 | 目的                                   |
| ---------------- | -------------------------------------------------------- | -------------------------------------- |
| A：当前整段 Seed | 继续调整可编辑全局声音提示词，保留中文导演说明和英文对白 | 验证整集表演连贯性、人物互动和整体效率 |
| B：逐句克隆      | 按确认 cue 和角色参考音逐句生成，再按绝对时间轴合成      | 验证单句可控性、漏句恢复和局部重做成本 |

对比只看真实结果：台词完整率、角色音色稳定性、情绪准确度、时间窗贴合度、局部修改难度、失败重试粒度、总耗时和调用成本。对比完成前不删除任一路线，也不改动共同的字幕、角色、音频分离、混音和烧录组件。

## 来源与追溯

- 事实来源：2026-08-10 当前会话，用户确认“人工确认稿是唯一最正确的”，并确认本轮分组克隆调整“非常成功”；指纹：未计算（会话不是项目内文件）。
- 实现提交：Git `333ccc312d0fa4b30a98c5753bedc3005cdfa4a4`（分组行与字幕序号对齐）；Git `8e44022b88345c2a07b7f9dfe9019a9ddf3a3077`（人工确认稿作为配音源）。
- `src/runtime/videoTranslation.ts`：`sha256:f3e6461dd3574fee71105daf01688da19c28d07128e2a1c03f0555d734dcc0c6`。
- `src/store/mediaTask.ts`：`sha256:3f83a0e25765ba05d2836dc1159538c3712b28ca32c3dd323f20ee389cbbe6cc`。
- `src/views/Home/components/VideoManage.vue`：`sha256:ddf9873a22e723be462c6f498123dd03b665eec4ee46e8432715cce818e27646`。
- `src/views/Home/components/VideoRender.vue`：`sha256:a9f1f88b2a64dca462dfefe307774d1fc18637ceb2b49e0ed130421952b22529`。
- `src/views/Home/index.vue`：`sha256:0be07a9d729c9a1b2354884120271667e3bd86d6d03cfee5b1cf9a42b4738969`。
- `src/runtime/videoTranslation.test.ts`：`sha256:9aa84f6145f84d2c8bd509df0872bea151665c6f29a15f823863b57e5993ec56`。
- 已处理范围：分组克隆的字幕序号、手动分组、提示词编辑、当前配音版本失效、人工确认稿唯一事实源、错误提示、桌面验收、自动测试与 macOS Universal 构建；未将尚未完成的真实音色质量 A/B 写成最终结论。
- 记录时间：2026-08-10 CST。

- 事实来源：2026-08-16 当前会话，用户确认有字幕视频 OCR 测试结果“非常成功”；指纹：未计算（会话不是项目内文件）。
- `runtime/funasr/runtime.py`：`sha256:7E7AD29DAA34D27BD6EEDFCDA795D831363961163270B685A8403FA1C161562A`。
- `electron/funasr-installer.ts`：`sha256:92EEE47FA588C9C243042112D23768FA05E919591BB44CE1AC397B1FAD64AF96`。
- `electron/video-translation-ocr.ts`：`sha256:925432A1846D09F82D1432A837F4477F6E9F6B26421C2FC3F703A4DBE0610C7B`。
- `electron/video-subfinder.ts`：`sha256:83AB8D82AB6DE363944F8194326B56BB636DE75F574ED92D189F6DF58515F786`。
- `src/views/Home/index.vue`：`sha256:09322A3FD2B87C34A8EE93FDE3A4EBF01A0026CF8B58FCDBA4ED9F5BEA1409E5`。
- 已处理范围：字幕来源三分法、SRT 优先、硬字幕 OCR 主链、VideoSubFinder 安装与运行边界、有效画面 ROI 兜底、角色识别与字幕事实源分离；未把后续 UI 文案精简和 ROI 下三分之一区域调优写成已完成。
- 记录时间：2026-08-16 CST。

- 事实来源：2026-08-09 当前会话，用户确认字幕拆分成功、时间戳满意、抽帧校准效果好，并确认画面人物识别优于声音人物识别；指纹：未计算（会话不是项目内文件）。
- 现行实现基线：Git `99fa67c`，tree `e6873847885767b5071e8441f17bffe0f18fb4f7`。
- `docs/视频翻译工作台-扒片按钮执行流程.md`：`sha256:0f245b991378006260030767eb5f37af118548874306d4f419c184dac2e839fa`。
- `electron/cloud.ts`：`sha256:7b52b5de25189ea95321444cbb7444fdcff84c547facab4af16b344964039084`。
- `src/runtime/videoTranslation.ts`：`sha256:0ef4708cfd672cbd41e90dc5eedef5c97ba2911e83561eded927ff46a522845e`。
- `src/views/Home/index.vue`：`sha256:1ecfa1575080914f655994da8f45cbeef57886760d638c88fe26dea7d977f57b`。
- `src/views/Home/components/VideoTranslationWorkspace.vue`：`sha256:b39e17bce9c998762f38a8e580576975b31b49dd737eb8a8c47856f0434db580`。
- `src/runtime/videoTranslation.test.ts`：`sha256:81324a8902e49da673651013f6d54846c8f04f0a48f6c1001aab1053a179cf49`。
- `src/runtime/cloud.mock.test.ts`：`sha256:e87801abc2702d79c019389422df0430349b7f71934837bd751ef6c845bf451f`。
- 已处理范围：从弃用旧切片扒片流程开始，到 FunASR、字幕拆分、抽帧文字/人物校准、人工确认稿、角色绑定、翻译门禁和美式本地化 Prompt 的本轮已确认结论；配音 A/B 与新翻译 Prompt 的真实质量仍未写成已验收结论。
- 记录时间：2026-08-09 CST。
- 事实来源：2026-08-07 当前会话，用户确认“整体是运行完整了”；指纹：未计算（会话不是项目内文件）。
- 实现基线：Git `2904b28`，tree `928369f2f7c8fcfad053af7c8fc58e40eb95a2a7`。
- `docs/tdd/13-视频翻译工作流TDD.md`：`sha256:a38be078a166421ecb327e8dd8dac2be637b2a92a0a0aea6bff2d353139c9a7a`。
- `src/views/Home/index.vue`：`sha256:ad10b8410696cc9aaf5bd218672fb7f8685f957e68659c509ff78b82617e62b1`。
- `src/runtime/videoTranslation.ts`：`sha256:87c8ff252f81a23abe53ce050e221ffc5dc5af215124bc2d0c2c2c2b13d7e371`。
- `electron/video-translation.ts`：`sha256:451865f9f37794507c95904d4af5a68fc3137a37c6880893a831a86e0eca320c`。
- `skills/jc-doubao-seed-audio/SKILL.md`：`sha256:2746dcb96578ff2c1358e382b2eeb69bbbb9616d4e5908866e43128ea0a637e5`。
- 已处理范围：本轮视频翻译真实运行结论、已验证工程经验和下一步配音路线决策；未把尚未对比的质量判断写成结论。
- 记录时间：2026-08-07 14:21:03 CST。
