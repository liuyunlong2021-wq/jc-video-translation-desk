# 2026-08-05 项目存储一致性修正

## 巡检范围

`CLAUDE.md`、`hot.md`、现行 `架构/`、`开发/`、`运维/`、`排障/`、`来源索引.md`、TDD-12 与对应代码/测试证据。历史 `log.md` 和明确标注为历史基线的页面只校验语境，不改写当时事实。

## 修正记录

| 问题 | 处理 | 结果 |
|---|---|---|
| 来源索引的 TDD-12 汇总仍为 `149/149` | 同步为 `151/151` 及“所选目录本身就是项目根” | ✅ 已修 |
| 现行运维页仍指向 `userData/media-runs` | 改为用户项目根，明确 `userData` 只保留应用级数据 | ✅ 已修 |
| 本地启动命令仍使用旧应用名 | 改为 `点一点.app` | ✅ 已修 |
| 3D 预演 SDD 仍使用旧项目路径 | 改为用户项目根下的 `episodes/<episodeId>/blocking/` | ✅ 已修 |
| 创作参数页仍声称兼容迁移旧测试项目 | 同步 TDD-12 的“不兼容、不迁移” | ✅ 已修 |
| 旧构建排障页的历史路径易被误读为现行规则 | 保留历史证据，增加历史边界和现行运维回链 | ✅ 已修 |

## 证据与剩余边界

- 测试证据：`151/151`，`sha256:9a0530e85a3e`。
- 构建证据：macOS universal APP/DMG 成功，`sha256:ef1567fea46a`。
- 修正后已运行 Wiki 校验：`docs/wiki` 稳定入口存在、双链目标可达，校验通过。
- 用户桌面实测和 Windows 路径仍未验证，不写成已通过。

关联：[[../架构/项目Wiki与资产主导工作流]]、[[../排障/新建项目误建日期子目录]]、[[../hot|当前状态]]。
