# Wiki Log

## 2026-08-17

- 分组克隆改为单按钮两层 Prompt 流水线：唯一按钮“批量生成全部分组配音”内部先生成整集全局声音基底，再让 `jc-luyinpeng` 基于全局基底、完整剧本、当前分组和参考音逐组生成提示词，最后批量生成组音频；不新增全局配音按钮，角色参考音人工确认门禁保留。删除“重新生成当前组”入口和局部确定性弱提示词自动填充；分组校验不再强制“时长为 X 秒”首句。2026-08-17 追加：全局基底、分组提示词和分组音频均改为最多 3 个并发，右侧显示阶段进度，失败后再次点击同一按钮只补缺失或失败项；用户截图里的“分组时长提示不正确”来自未重启的旧 Electron 主进程。`pnpm exec vue-tsc --noEmit`、`pnpm test src/runtime/videoTranslation.test.ts`、`git diff --check` 通过。
- 视频翻译角色参考音 Prompt 改为程序内两行短格式：`身份 + 目标语言年龄性别 + 强声音特征`，再接中性稳定示例台词；不再调用 `jc-voice-design` 或 `jc-doubao-seed-audio` Skill 生成角色参考音提示词。新增越南男服务员样例测试与禁用 Skill 路由静态断言，`pnpm exec vue-tsc --noEmit`、`pnpm test src/runtime/videoTranslation.test.ts`、`git diff --check` 通过。
- 固定 Seed Audio prompt-only 请求合同：生成声音时只提交 `model` 和最终提示词 `input`；客户端删除 `voice`、`response_format` 字段及男女性别到 `echo`/`nova`/`alloy` 的映射，避免与“模型靠提示词生成声音”的产品设计冲突。`pnpm exec vue-tsc --noEmit`、`pnpm test` 和 `git diff --check` 通过。
- 更新 [[开发/视频翻译工作台端到端验收]]：记录字幕工作台新增越南语、泰语、印尼语、马来语（马来西亚）目标语言，用户真实测试确认成功。
- 记录配音工作台第二页减法：视频翻译产品隐藏“全局配音”入口，主路径收口为 `角色配音 -> 分组克隆 -> 成片工作台`；不再读取旧的人工全局提示词，分组音频缓存按提示词和参考音指纹复用。
- 新增 `docs/tdd/16-东南亚字幕翻译语种TDD.md` 与 `docs/tdd/17-配音工作台分组克隆主路径TDD.md`；`pnpm test` `210/210`、`pnpm exec vue-tsc --noEmit`、`git diff --check` 通过。
- 更新 [[开发/视频翻译工作台端到端验收]]：记录用户确认 OCR 与画面识别人物均“非常成功”，并沉淀字幕工作台减法收口方案。
- 字幕工作台主链改为选择字幕来源后点击获取字幕，系统自动获取字幕并继续做画面人物识别；“画面识别人物”不再作为主操作按钮。
- 右侧操作区精简：`上传识别视频` 改为 `上传视频/更换视频`；选择 `导入 SRT` 时主按钮显示 `导入 SRT`；“大模型语义校准”移入“高级功能”折叠。
- 字幕表格精简：删除 `FunASR 原文` 展示列，保留底层原始识别证据；默认不再提供批量角色绑定开关，系统自动绑定可确定的单人画面，未匹配项由用户逐条修正。新增“关联配音分组”；手动改角色会清空旧配音分组，避免混角色分组残留。
- OCR 体验补强：进度提示简化为 `正在获取画面字幕`；有效画面 OCR 兜底区域收窄到下方约三分之一。`pnpm exec vue-tsc --noEmit`、目标 UI/合同测试 `44/44` 和全量 `pnpm test` `209/209` 通过。

## 2026-08-16

- 更新 [[开发/视频翻译工作台端到端验收]]：字幕来源收口为 `导入SRT`、`上传有字幕视频`、`上传无字幕视频`。已有 SRT 直接作为权威字幕；有字幕视频使用 `VideoSubFinder -> RapidOCR/RapidVideOCR -> SRT`；无字幕视频继续走 FunASR ASR。
- 记录 Windows 硬字幕 OCR 真实样片经验：VideoSubFinder 默认底部区域会漏掉竖屏短剧嵌入横屏画面的字幕，已增加有效非黑画面区域检测和中下区域 RapidOCR 兜底；用户确认本轮测试结果“非常成功”。
- 更新 [[运维/本地构建与发布]]：沉淀 RapidOCR/RapidVideOCR/VideoSubFinder 一键安装与扫描复用经验，记录 SourceForge 下载需明确 zip、设置 User-Agent、校验 zip 魔数，VideoSubFinder 运行目录和 Windows 异常退出码不能作为唯一失败依据。
- 本轮版本号已提升到 `1.2.3`，但按测试策略未重新打包安装包；已验证 `pnpm exec vue-tsc --noEmit`、`pnpm test` 和 `python -m py_compile .\runtime\funasr\runtime.py`。后续待做两个小优化：OCR 进度文案简化和 OCR 截图区域调窄。

## 2026-08-11

- 修复 Windows 用户下载 APP 后点击“一键安装”出现 `spawn ffprobe ENOENT` 和 `uv.exe 安装失败` 的首装链路：安装包随包提供 `ffmpeg.exe`、`ffprobe.exe` 和按架构隔离的 `uv.exe`，大模型仍由一键安装下载，不把约 2 GB FunASR / 人声分离模型打进安装包。
- 并发审计确认方案不过度设计，但发现 FunASR 模型目录判断与 `runtime.py` 不一致、`uv` 产物路径需按架构校验、`ffmpeg-static` 干净构建需硬校验等真实风险；已修复模型目录合同、构建前媒体工具校验、`uv --version` 校验、旧 uv 路径迁移与 postinstall 传 `undefined` 镜像变量问题。
- Windows x64 正式构建、`207/207` 自动测试、TypeScript 类型检查和 `git diff --check` 通过；产物 smoke check 确认 `resources/runtime-tools/uv/x64/uv.exe --version`、`ffmpeg.exe -version` 和 `ffprobe.exe -version` 均可执行。提交 `979bff0 Fix Windows first-run runtime tools` 已推送到 `origin/main`。
- 更新 README 和 [[运维/本地构建与发布]]：普通用户不再需要自行安装 Node.js、Python、FFmpeg、ffprobe 或 uv；源码安装步骤仅作为开发者备用。后续仍需锁定 `torch/torchaudio` 版本、为 ONNX 模型增加校验，并补充安装诊断报告。

## 2026-08-10

- 更新 [[开发/视频翻译工作台端到端验收]]：人工确认稿固定为配音、提示词与成片字幕的唯一事实源；字幕内容、角色、时间或语种变化会废弃旧提示词和采用配音，单纯配音分组调整只影响分组配音。分组页移除重复视频预览，当前组三段式提示词移至右栏并保持可编辑。`206/206`、类型检查、macOS Universal 签名与 DMG 校验通过；用户确认本轮调整成功。
- 新增 [[排障/Windows字幕乱码与参考音按钮缺失]]：根据 Windows 实机确认，在共享 FunASR Python 子进程中固定 UTF-8 输出环境；确认参考音按钮不再受翻译模式隐藏并由合同测试锁定。Mac 类型检查与 `200/200` 通过；Windows 新安装包真实回归待执行。
- 完成视频翻译独立 APP 收口复检：默认开发、构建、应用身份和安装包不再暴露“点一点”；三个配音按钮使用统一 API Key/最终配音门禁；`workspaceEntry`、Electron、SQLite 与 FunASR 数据根统一；旧目录非覆盖复制缺失数据；GitHub Release 改读 `release-translation`，Windows 固定本机 Runner 构建。`200/200`、类型检查、差异检查、macOS Universal 构建与正式 APP 桌面核验通过；同一 Universal DMG 支持 Apple M 系列和 Intel Mac。
- 沉淀整段配音对齐经验：FunASR 整块英文词级时间 + 确认英文稿字符级单调匹配 + FFmpeg 从原始高质量配音块裁句；删除固定小片和多模态猜 cue 的旧思路。极短语气词保留前后真实词之间的完整声音，低匹配度直接阻止成片。`pnpm test` `199/199`、类型检查和差异检查通过。
- 重写 README 安装部署：说明源码安装、FunASR 环境/模型不随安装包发布，并分别给出 Apple Silicon、Intel Mac、Windows 的 Git、Node.js、uv、pnpm、模型探针和启动步骤；明确 Apple Silicon 已实测，Intel/Windows 首装仍待干净机器验收。

## 2026-08-09

- 更新 [[开发/视频翻译工作台端到端验收]]：旧的压缩、切片、逐片多模态扒片路线退出；沉淀 FunASR 固定时间戳、真实字词时间戳拆分、中点抽帧复用、画面人物优先、人工确认稿唯一真值和面向美国观众的影视本地化翻译合同。记录真实样片已确认的时间戳、拆分和抽帧文字效果，以及 `99fa67c`、`199/199` 测试、类型检查和差异检查证据；新翻译 Prompt 的真实质量仍待继续体验。

## 2026-08-08

- 写入并完成 [[巡检报告/2026-08-08视频翻译三工作台收口]]：确认三工作台主链不增加 Whisper、智能切片或额外模型裁决；角色、时间、提示词、配音版本和成片 ID 衔接已修复，重复状态与重复记录已收口。`198/198`、TypeScript、Vite/Electron/macOS universal 构建和 `git diff --check` 通过；真实样片验收待执行。

## 2026-08-07

- 固定 Seed 参考音身份最终合同：只使用产品 `voiceProfileId`，正式生成请求直接携带参考音数据和格式，删除独立声音注册链路和额外声音 ID 分支。修复参考音试听，恢复失败前已成功生成的风千雪 10.032 秒参考音并设为当前选择；旧版本仍可选。`197/197`、类型检查、差异检查、macOS universal 构建与桌面核验通过。
- 用户完成视频翻译工作台一次真实全链路运行：上传、扒片、字幕与译文确认、角色参考音、目标语言配音、人声与背景声处理、字幕烧录和成片输出均已闭环。
- 当前实现基线为 Git `2904b28`；结论是保留现有主链，不做大规模架构调整。真实跑通不等于全部质量指标已最终验收，后续只按样片结果做局部修复。
- 下一步使用同一集确认字幕、角色和参考音进行配音 A/B：一条继续优化整段 Seed 全局声音提示词，另一条新增逐句克隆；根据完整率、音色、情绪、时间、重试、耗时和成本决定最终路线。详见 [[开发/视频翻译工作台端到端验收]]。

## 2026-08-05

- 完成 TDD-12：新项目由用户选择可读存储目录，应用级注册表保存稳定 `projectId -> rootPath`；全部项目文件继续使用相对路径，旧 `media-runs` 测试项目不迁移、不兼容。
- 资产版本拆分来源与用途：搜索、上传、AI 生成均可通过 `activeVersionId` 直接成为当前使用图；分镜、视频和 Wiki 只消费当前版本，上传旧图无需重新付费生成。
- 项目创建/打开 UI、IPC、声音库反链、FFmpeg、任务恢复及 Wiki 投影已统一到项目根解析器。全量测试 `149/149`（含移动目录后重新打开和未注册缓存 ID 保存门禁）、类型检查、差异检查和 macOS universal APP/DMG 打包通过；桌面人工和 Windows 路径待验收。
- 纠正新建项目的目录语义：所选空文件夹本身直接成为项目根，不再把它当成父目录并追加日期子目录；非空目录和已有项目目录均有明确门禁。全量测试 `151/151`、类型检查、差异检查和 macOS universal APP/DMG 打包通过；新目录选择器仍待用户桌面验收。
- 完成项目存储 Wiki 一致性巡检与修正：同步现行运维数据位置、3D 预演剧集目录、应用名、TDD-12 验证数和旧项目不迁移边界；历史页保留原证据并增加历史标识。巡检记录见 [[巡检报告/2026-08-05项目存储一致性修正]]。
- 修复旧 Pinia `runId` 阻塞“新建项目”和“打开已有项目”：只有注册项目才能执行显式或自动 `cloud-save-state`，未注册状态直接进入新建/打开流程。根因与回归见 [[排障/未注册旧项目阻塞新建与打开]]。

- 确认将视频反推剧本正式放入「点一点」，定位为“文稿”阶段的第二种项目输入路线，不开发独立 App 或另一个 Electron 窗口。
- 新增 TDD-11：固定“原片 -> Faster-Whisper 母时间轴 -> Gemini 3.6 Flash 原生视频分析 -> 确定性剧本渲染 -> 现有文稿编辑与确认”主链。
- 旧“视频配音翻译模式直接跳到配音字幕工作台”设计退出现行方案；视频反推必须先进入文稿人工确认，之后才复用 Seed Audio / 逐句后配和完整创作下游。本轮仅更新设计与 Wiki，尚未实施代码或运行测试。
- 确认 Seed 声音优先路线新增独立可编辑节点：项目总监与声音资产完成后，先执行整段配音安排，再由 `jc-doubao-seed-audio` 生成 `声音导演稿.md`；用户保存后“生成完整声音轨”只能原样消费该稿。确认文稿、任务安排、声音导演稿和画面导演分镜保持四层分离。本轮仅更新设计、Skill 与 Wiki，代码待执行。
- 完成 TDD-10 代码主链：接入 Seed Audio 官方客户端、安全密钥、产品 `voiceProfileId`、角色 Seed 基准音、确定性整段任务、多任务混轨、自动 Faster-Whisper 时间轴/SRT、导演门禁和配音字幕工作台完整轨消费；全量测试 `147/147`、类型检查和差异检查通过，真实 Seed 官方请求与完整项目成片待验收。
- 扩展 TDD-10：确定不新建独立 Electron APP 或第二套配音工作台，而是在现有 Electron 应用增加项目级“视频配音翻译”模式。
- 新模式保留顶部项目/剧集选择器；左栏只负责上传当前集源视频和一键分析，Whisper 与 Gemini 形成唯一 `editing-timeline.json` 后直接复用现有配音字幕工作台。
- 固定项目/剧集边界：工作模式属于项目，角色声音为共享资产；源视频、转录、时间轴、字幕、Seed Audio 配音和成片按集隔离。当前仅更新设计与 Wiki，尚未实现或执行测试。

## 2026-08-04

- 完成 TDD-06：人工剪辑点、中文字幕、英文翻译和逐句中英文配音均写入当前项目；IndexTTS2 生成逐句 WAV、本集 WAV 与 `对白资产.json`，工作台支持逐句试听。
- 完成 TDD-07：右栏接通三种音频模式、中文/英语成片选择和四个真实后期动作；Spleeter 生成双 stem，原人声只排除不删除，最终音轨只混 `instrument.wav` 与当前语言配音。
- FFmpeg 最终烧录只消费确定音轨和当前字幕，并写入 `wiki/成片/episode-001.md` 与 `wiki/制作/episode-001.md`。全量测试 `136/136`、类型检查和差异检查通过；本机真实 Spleeter 与桌面右栏已验收，证据摘要 SHA-256 为 `5f458ece1727fff7`。
- 尚未用真实完整创作项目完成付费 Gemini、IndexTTS2、音频处理和最终成片的端到端验收。
- 完成 TDD-05 配音字幕工作台骨架：七阶段第六阶段正式改为“配音字幕”，剪辑时间轴就绪后进入独立 `dubbing` 视图；中栏提供原视频预览、逐素材时间轴滑块和六列表格，右栏列出八个后期动作。
- 八个后期动作当前均明确为待接入禁用态；本轮未实现或伪造 IndexTTS2 逐句配音、翻译、人声分离、混音和字幕烧录。全量 `pnpm test` 为 `129/129`，类型检查和差异检查通过，测试输出 SHA-256 为 `0d004ead2f7b`。
- 根据桌面验收修正工作台可见性：全部视频完成后即可提前查看配音字幕工作台；缺少 SRT 或剪辑时间轴时只展示等待状态，实际动作门禁不放宽。
- 优化配音字幕工作台布局：表格独立纵向滚动并保持表头可见；竖屏宽栏使用左侧预览/右侧表格，横屏使用上下布局，窄栏回退上下布局。时间轴未完成时禁用滑块，右栏移除重复返回按钮。
- 根据桌面复核统一横竖屏工作台为左侧预览/右侧表格；进入配音字幕时自动收起左侧项目设置，左右栏均可从顶部切换，两栏收起即最大空间模式。点击素材行会从该行剪辑起点播放并在终点暂停。
- 完成 TDD-03：移除视频生成后的自动 Gemini 调用，新增用户主动“生成 SRT”按钮；本机 Faster-Whisper 为每条唯一素材原子写入 Whisper JSON 与 SRT，Grok 共用视频只转录一次，单条失败不覆盖其他成功素材。
- 真实加载 `faster-whisper 1.2.1`、`ctranslate2 4.8.1` 和 1.5GB large-v3-turbo；1 秒静音视频返回 `duration=0.96`、空 `segments`，验证无人声素材是合法成功。
- 完成 TDD-04：新增“生成剪辑时间轴”门禁；`gemini-3.6-flash` 合同读取原视频、确认剧本、同素材全部完整分镜提示词、Whisper JSON/SRT，同一 Grok 物理视频只提交一次。
- Gemini 输出会校验剪辑区间、cue、角色和画面内对白窗口；无效、遗漏或普通模型失败保留完整素材并 `needsReview=true`。整集结果立即原子写入 `wiki/剪辑/episode-001/editing-timeline.json`，无需先运行 FFmpeg。
- 全量 `pnpm test` 为 `128/128`，`pnpm exec vue-tsc --noEmit` 与 `git diff --check` 通过。测试输出 SHA-256 为 `dad80fafca24`；两个空成功输出的 SHA-256 均为 `e3b0c44298fc`。
- 未执行真实付费 Gemini 调用；本地浏览器 URL 安全策略阻止自动化可视验收，因此 UI 只由类型检查和合同测试验证，未写成人工/桌面验收通过。

## 2026-08-03

- 完成 TDD-02：设置页新增 Qwen3-TTS / IndexTTS2 本地引擎选择；IndexTTS2 具备真实检测、受管启动、重复启动去重、停止和 APP 退出清理。
- 本机桌面验收确认 IndexTTS2 通过 `uv run webui.py` 加载，`/config` 健康检查后 UI 显示“服务运行中”，停止后 `7860` 端口与子进程均清理；修复 Electron 22 不支持现代 `fetch` 健康检查的运行时问题。
- 剧情角色音色绑定只接受项目总监白名单角色和已审核、商业授权、可克隆且拥有确认 IndexTTS2 情绪包的声音档案；声音页与角色页形成双链，旁白和逐句配音未提前实现。
- TDD-02 定向测试 `54/54`、全量测试 `122/122`、TypeScript 类型检查、`git diff --check` 和桌面真实启停验收通过。
- 完成 TDD-01：项目总监前置判断 `narration-promo` / `drama`，用户可在确认前修改；下游只读取已确认 `ProjectDirectorPlan.productionRoute`。
- 路线变更确认时清除路线相关下游，同时保留项目总监白名单内已有资产 ID、设计和媒体版本；项目总监与制作路线 Wiki 必须先写入成功再更新 Store。
- 桌面验收定位并修复项目总监页复用资产阶段标记后误显示资产按钮栏的问题；修复后右栏正确显示“生成项目总监方案”。
- TDD-01 定向测试 `54/54`、全量测试 `118/118`、TypeScript 类型检查和 `git diff --check` 通过；旁白、Whisper、Gemini、IndexTTS2 与配音字幕工作台仍留给后续 TDD。
- 完成 TDD-00：建立共享生产合同与配音字幕状态机，将 `editing-timeline.json` 升级为同时保留 Gemini 原始点和当前采用点的 `schemaVersion: 2`，FFmpeg 只执行当前采用点。
- 定向合同/时间轴/FFmpeg 测试 `11/11`、全量测试 `114/114`、TypeScript 类型检查和 `git diff --check` 通过；本轮未接入 Whisper、Gemini、IndexTTS2、UI 或端到端成片链路。
- 同步 [[架构/逐镜智能剪辑与声音成片]]：项目总监前置分流旁白宣传片/剧情片；宣传片旁白时间轴自动生成，剧情片资产阶段只绑定角色音色。
- 固定素材证据链为 Faster-Whisper 生成 SRT，再由 `gemini-3.6-flash` 结合确认剧本、完整分镜提示词和原视频直接确定 `speakerId`、戏剧动作与剪辑点；删除 Sherpa 说话人聚类。
- 配音字幕工作台只保留逐条剪辑滑块、生成中英文配音、翻译、按需人声处理和烧录成片；剪辑点与中文字幕自动持久化，不增加保存、确认、最终混音试听或独立导出按钮。
- 更新 [[CLAUDE]]、[[hot]] 和 [[来源索引]]；本轮只有 SDD 与 Wiki 变更，没有代码实现、测试、构建或端到端成片证据。
- 2026-08-05 声音约束纠正：VEO/Grok 继续按原分镜提示词生成对白、旁白、音乐、环境声和动作音效；后期才决定采用视频原生声音、Seed 完整声音轨或逐句后配。同步修正 `jc-script-storyboard`、`jc-context-revision`、`jc-storyboard-image`、视频修改校验和 Markdown 分镜编译器，移除强制“不得生成人声/无背景音乐”约束；`pnpm test` `141/141` 通过。

## 2026-08-02

- 将逐镜剪辑模型从本地 Qwen2.5-Omni-7B 修正为 `gemini-3.6-flash`：每条原始分镜视频连同对应分镜 JSON 直接提交，不抽帧；`ffprobe` 校验、`editing-timeline.json` 和 FFmpeg 执行合同保持不变。
- 将长期产品讨论提炼为项目资产主链、Pinterest 参考图、三栏任务交互和逐镜智能剪辑四个专题。
- 明确角色、场景、道具三类资产；产品统一归入道具，资产设计 JSON 是生图正式合同。
- 记录联网图与用户上传图同属参考集合、删除后只补空缺、批量生成只处理未完成项。
- 记录项目级媒体任务的停止、继续查询、继续下载和重新生成语义，防止局部任务锁死项目或重复付费。
- 确认 Gemini 逐镜直读、`editing-timeline.json`、IndexTTS2、多种原声策略和字幕成片方案仍待执行。
- 将 2026-07-30 的单参考图、先配音和固定截取方案标注为历史基线，保留原始验证证据。
- 本轮只更新 Wiki，未执行构建、自动测试或真实付费媒体生成。

## 2026-07-30

- 使用 `jc-everything-wiki` 建立开发项目 Wiki 骨架。
- 记录分段式 AI 短视频工作流、固定模型合同、单核心参考资产、媒体持久化和 FFmpeg 统一配音规则。
- 记录视频接口、滚动布局、素材恢复、旧 Electron 进程与动态分块不一致等已验证经验。
- 完成 39 项自动测试、类型检查和未签名通用应用打包；发布证书签名仍受本机钥匙串 `errSecInternalComponent` 影响。
- 记录人工验收：最终视频合成成功；提交 `57c48b6` 已推送到 `origin/main`。
- 审计根目录项目文档：重写 `README.md` 为当前 AI 原生工作流说明，在 `CHANGELOG.md` 增加未发布版本记录，并给两份 SDD 标注现行与历史状态。
- 查证 Google Cloud 官方 Veo 文档，记录图生视频、4/6/8 秒、摄像机运动、时间元素和剪辑术语能力及高级控制可靠性边界。
- 确定并实施自动、慢、中、快四档独立镜头节奏，创建 `docs/镜头节奏控制SDD.md`。
- 移除未使用的 `vite-plugin-vue-devtools`，并为 Pinia 测试入口补充最小 `localStorage` 预加载，修复 Node 25 兼容问题。
- 执行《创作参数与品牌视觉升级 SDD》：增加 9 个风格预设、动态时长、旧数据迁移、赚钱短片品牌、SVG/PNG Logo 和绿色主操作主题。
