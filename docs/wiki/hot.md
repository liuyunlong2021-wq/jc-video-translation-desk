# 热缓存

> 更新：2026-08-04 | 阶段：TDD-05 配音字幕工作台骨架已完成

## 当前结论

- 项目总监已在声音生成、资产和分镜之前形成真实门禁，负责判断 `narration-promo` 或 `drama`；AI 初判后用户可以修改并重新确认。
- 已确认 `ProjectDirectorPlan.productionRoute` 是路线唯一事实源；存在待确认稿时下游关闭，确认后才失效声音、分镜、视频与成片，并保留白名单内已有资产版本。
- 创作项目确认路线时先写 `wiki/项目/项目总监.md` 和 `wiki/项目/制作路线.md` 双链页面，再暴露确认状态。
- 宣传片以正式旁白时间轴为第一时间轴，旁白生成后自动运行 Faster-Whisper，不增加按钮。
- 剧情片资产阶段只绑定 `speakerId -> voiceProfileId`；`editing-timeline.json` 就绪后，由用户点击“生成中文配音”才调用 IndexTTS2。
- 每条原始视频先由 Faster-Whisper 生成素材 SRT；`gemini-3.6-flash` 再读取原视频、确认剧本/文稿、完整分镜提示词和 SRT，直接输出角色身份、动作和准确剪辑点。
- 不使用 Sherpa 说话人聚类。Sherpa-ONNX 只参与按需执行的 Spleeter 人声分离链路。
- 全部视频素材完成后即可提前进入配音字幕工作台查看原视频、六列表格和逐行时间轴滑块；时间轴未完成时只读显示“等待时间轴”，所有后期动作继续禁用。
- 顶部七阶段为：文稿、资产、分镜、分镜图、视频、配音字幕、成片。“烧录配音和字幕”就是生成成片。
- `rh-grok-image-video` 已真实验证九宫格分镜板、多图参考、人物/动物一致性和片内切镜；官方 `6-30` 秒合同保留，但不是全部时长逐档实测结论。

## 已验证 / 未验证

- 已验证：Grok 多图参考与片内切镜；Faster-Whisper、IndexTTS2 多情绪包、Sherpa-ONNX + Spleeter 模型和 FFmpeg 复用路径已有本地证据。
- 已完成 TDD-00：统一双路线、素材转录、`editing-timeline.json schemaVersion: 2`、配音/音频记录类型、十项工作台操作门禁和最小失效规则；FFmpeg 已只读取当前采用剪辑点。全量测试 `114/114` 和 TypeScript 类型检查通过。
- 已完成 TDD-01：项目总监 Skill/解析器、双路线修改与确认、下游门禁、资产白名单保留、双 Wiki 写入和最小 UI 已落地；桌面验收修复项目总监页误显示资产按钮栏。定向测试 `54/54`、全量测试 `118/118`、类型检查和 `git diff --check` 通过。
- 已完成 TDD-02：设置页可选择 Qwen3-TTS 或 IndexTTS2；IndexTTS2 真实检测、`uv run webui.py` 启停、健康状态和退出清理已落地。剧情角色只可绑定项目总监白名单内角色及已确认的商业可用 IndexTTS2 声音包，并写角色/声音双链。全量测试 `122/122`、类型检查、差异检查和桌面真实启停验收通过。
- 已完成 TDD-03：视频生成不再自动调用 Gemini；用户点击“生成 SRT”后，本机 Faster-Whisper 逐唯一素材生成 JSON/SRT，同一 Grok 视频只转录一次。`faster-whisper 1.2.1`、`ctranslate2 4.8.1` 和 1.5GB large-v3-turbo 已真实加载，静音素材正确输出空 cues。
- 已完成 TDD-04：素材 SRT 全部就绪后开放“生成剪辑时间轴”；Gemini 合同一次读取同一物理视频及其全部分镜、确认剧本、Whisper JSON/SRT，聚合并立即写入唯一 `editing-timeline.json`。无效、遗漏或普通模型失败保留完整素材并标记 `needsReview`。
- 已完成 TDD-05：七阶段第六阶段进入独立 `dubbing` 工作台；中栏原视频预览、六列表格和逐行滑块已落地，右栏八个后期动作均以禁用态明确标注待接入。
- TDD-05 桌面 UI 已验收：长表格独立滚动、表头吸顶，横竖屏统一为左侧预览/右侧表格；进入时左栏自动收起，左右栏均可切换并支持最大空间模式。点击行会按该行剪辑起止点播放；等待时间轴时滑块禁用。
- TDD-05 全量测试 `129/129`、TypeScript 类型检查和 `git diff --check` 通过；真实 Gemini 付费调用未完成，不能写成端到端验收通过。
- 未验证：逐句配音、滑块持久化、人声分离、混音、字幕和最终成片尚未完成代码闭环。
- 尚未进行真实端到端成片验收；TDD-00 只证明共享基础可用，不能把后续模型与 UI 功能写成当前生产能力。
- 当前工作区仍有未提交 SDD 和其他用户文件，后续实施不得覆盖无关变更。

## 下一步

- 下一项继续先写 TDD，把当前行时间轴滑块持久化回唯一 `editing-timeline.json`；不得把 IndexTTS2、人声分离或 FFmpeg 烧录混入同一 TDD。
- 实施时每个实际按钮必须有真实后端能力、失败状态和对应 Wiki 产物；完成后用两个新项目分别验收宣传片与剧情片。
