# 音色库 P0 盘点报告

- 扫描时间：2026-08-01
- 可识别音频：2,234
- 唯一 SHA-256 档案：1,104
- 完全重复组：1,104
- 音频格式：MP3 1,056，WAV 1,178

原始资源未被移动、改名、转码或删除。所有初始档案均为待审核；真实人物来源默认 `local-only`，其余来源默认 `unknown`，不进入产品默认候选。

完整可重复扫描会在应用级 `userData/voice-library` 生成 `catalog.json`、`盘点报告.json` 和 Markdown 索引；其中只保存来源相对路径与 SHA-256，不记录机器绝对路径。
